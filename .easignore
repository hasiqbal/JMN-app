# Keep EAS uploads minimal and deterministic
# This file is independent from .gitignore and is used for build uploads.

# VCS and local tooling
.git
.vscode
.venv
.tmp-urdu-font

# Dependencies and generated output
node_modules
.expo
dist
web-build
android/.gradle
android/**/build
ios/build

# Local artifacts and docs not needed in build context
tomorrow-work-plan.txt
quran.py

# Firebase native config must stay local-only (gitignored) but still be uploaded for EAS builds.
!google-services.json
!android/app/google-services.json

